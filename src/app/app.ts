import { Component, signal } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-root',
  imports: [RouterOutlet],
  templateUrl: './app.html',
  styleUrl: './app.css'
})
export class App {
  // Alterado o título usando o Signal do Angular para o seu Café Bar
  protected readonly title = signal('Café & Bar Dev');
}
